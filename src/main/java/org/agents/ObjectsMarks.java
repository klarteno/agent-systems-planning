package org.agents;

public final class ObjectsMarks {
    enum BoxField {
        LETTER_MARK_INDEX,
        COLOR_MARK_INDEX,
        GOAL_ROW,
        GOAL_COLUMN,
        SOLVED_STATUS;
    };

    enum CoordinatesField {
        ROW_POS,
        COLUMN_POS;
    };

    enum AgentField {
        NUMBER_MARK_INDEX,
        COLOR_MARK_INDEX,
        GOAL_ROW,
        GOAL_COLUMN,
        SOLVED_STATUS;
    };
}
